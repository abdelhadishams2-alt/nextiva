# Screenshot Capture Guide

Screenshots are captured using Playwright after each phase milestone. Store files as:

```
{screen}-{state}-{breakpoint}.png
```

Example: `dashboard-default-desktop.png`, `generate-progress-desktop.png`

Run `/capture-screenshots` to automate capture.
